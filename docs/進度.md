# 目前進度

最後更新：2026-09-02（v1.0 維持不變；main 已覆蓋今日收工內容）

## 上次做到哪

v1.0 已在 GitHub：https://github.com/simon5823/novelsystem（標籤仍為 `v1.0`，未推進）。

同日後續已加上每日開發流程：對 Grok 說「開始今天作業」／「結束今天作業」。

系列結構：系列 → 部 → 卷 → 章 → 場景。人物、世界、時間軸、線索、詞彙全系列共用。

## 下一件建議做的事

1. 地圖釘點（地點資料已有，差畫布）
2. 角色行為推演沙盒
3. Windows 安裝套件（現在用 `npm run dev`）
4. DOCX／EPUB 匯出

## 注意

- 本機無 Rust，桌面殼是 Electron，不是 Tauri。
- 不連線呼叫模型；校稿用網頁版 Grok。
- 收工預設只推 `main`，不打新 tag、不升版本號。
